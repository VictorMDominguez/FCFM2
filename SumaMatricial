import  sys
arr = []
res = []
arr_temp = map(int,raw_input().strip().split(' '))
arr.append(arr_temp)
n = len(arr_temp)
for arr_i in xrange(n):
    arr_temp = map(int,raw_input().strip().split(' '))
    arr.append(arr_temp)
